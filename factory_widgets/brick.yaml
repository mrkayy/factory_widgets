name: factory_widgets

description: Accelerate your Flutter development with the Factory Widget Mason Brick—a powerful tool designed to generate reusable, standardized Flutter widgets swiftly and efficiently. Leveraging the Factory Design Pattern, this Mason brick ensures consistency and modularity across your codebase, making it easier to maintain and extend.

repository: https://github.com/mrkayy/factory_widgets.git

version: 0.1.1+3

# The following defines the environment for the current brick.
# It includes the version of mason that the brick requires.
environment:
  mason: ">=0.1.0-dev.52 <0.1.0"

vars:
  widget:
    type: string
    description: Name of your custom flutter widget
    default: TextBuilder
    prompt: What's the name of your custom widget?
